import React from 'react';
import Navbar from './components/Navbar';

function App() {
  return (
    <div className="app">
      <Navbar />
      <section id="home">
        {/* ...existing code... */}
      </section>
      
      <section id="about">
        {/* ...existing code... */}
      </section>

      <section id="experience">
        {/* ...existing code... */}
      </section>

      <section id="projects">
        {/* ...existing code... */}
      </section>

      <section id="contact">
        {/* ...existing code... */}
      </section>
    </div>
  );
}

export default App;
