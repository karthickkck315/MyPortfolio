import { ThemeToggle } from './ThemeToggle';

const Navbar = () => {
  return (
    <nav>
      {/* ...existing code... */}
      <ThemeToggle />
      {/* ...existing code... */}
    </nav>
  );
};

export default Navbar;
